# Copy this file to .env and fill in your API Gateway URL.
# The dashboard automatically reads live data when VITE_API_URL is set.
# Leave it blank (or omit .env entirely) to use the local mockData fallback.

VITE_API_URL=https://<your-api-id>.execute-api.us-east-1.amazonaws.com/prod/resources
